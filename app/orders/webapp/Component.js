sap.ui.define(["sap/fe/core/AppComponent"], (ac) => {
// sap.ui.getCore().getConfiguration().setLanguage("zh_CN");
  return ac.extend("orders.Component", {
    metadata: { manifest: "json" }
  })
}
);
